# Voice-verfiction-model
Voice Verification Demo is an AI-powered biometric authentication system that verifies a user's identity using voice. Built with Python and SpeechBrain's ECAPA-TDNN model, it generates speaker embeddings and compares them using cosine similarity to securely authenticate users in real time.
